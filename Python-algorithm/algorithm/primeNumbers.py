from algorithm import *
class PrimeNumber:
    pr = AlgorithmMethods()
    lower_range = int(input('Enter lower range : '))
    upper_range = int(input('Enter lower range : '))
    pr.prime_in_range(lower_range,upper_range)

    
